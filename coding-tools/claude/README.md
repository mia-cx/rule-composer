# Claude Code Rules & Skills

Pre-processed rules and skills with Claude Code-specific paths and references resolved.

Copy the `rules/` and `skills/` directories to `.claude/rules/` in your project (or merge with your existing rules/skills).
