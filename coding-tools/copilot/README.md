# GitHub Copilot Rules & Skills

Pre-processed rules and skills with GitHub Copilot-specific paths and references resolved.

Copy the `rules/` and `skills/` directories to `.github/instructions/` in your project (or merge with your existing rules/skills).
