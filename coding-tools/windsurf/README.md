# Windsurf Rules & Skills

Pre-processed rules and skills with Windsurf-specific paths and references resolved.

Copy the `rules/` and `skills/` directories to `.windsurf/` in your project (or merge with your existing rules/skills).
